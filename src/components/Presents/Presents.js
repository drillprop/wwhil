import React from 'react';
import { Wrapper } from '../../elements/Wrapper';
import { dark } from '../../utils/colors';

const Presents = () => {
  return <Wrapper background={dark} />;
};

export default Presents;
